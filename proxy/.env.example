PORT=8080
ROBOFLOW_API_KEY=replace-on-server-only
ROBOFLOW_MODEL_ID=your-project/1
# Confirm these against the exact model's published classes. Do not list unsupported PPE.
SUPPORTED_PPE=helmet,vest
# Comma-separated per-device access tokens; store on each device in Keychain.
PROXY_TOKENS=replace-with-a-long-random-token
